fastapi>=0.112.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
pydantic>=2.13.5
email-validator>=2.3.0
pydantic-settings>=2.0.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
python-multipart>=0.0.9
httpx>=0.27.0
pytest>=8.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.9
requests>=2.34.2

# Dev tooling
# Pinned exactly on purpose: CI gates `ruff check .`, and a floating version
# would let a ruff release add rules and turn the gate red with no code change.
# Bump it deliberately, in its own PR, with `ruff check .` green locally first.
ruff==0.16.7
# Coverage measurement (TA6-3): CI runs pytest with --cov; the floor is
# enforced via [tool.coverage.report] fail_under in pyproject.toml.
pytest-cov>=4.1.0
