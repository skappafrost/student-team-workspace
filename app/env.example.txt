# =================================================================
# STW frontend environment — copy to .env.local and fill in.
# =================================================================

# Backend API origin. Every BFF route under app/api/* proxies here, and the
# chat WebSocket connects to it directly from the browser.
# IMPORTANT: use the same host you open the app on. Cookies are host-scoped
# (ports ignored), so http://localhost:3000 pairs with
# http://localhost:8000 — 127.0.0.1 breaks realtime (see README).
NEXT_PUBLIC_API_URL=http://localhost:8000

# =================================================================
# Error tracking (Sentry) — all optional. App runs fine without it.
# =================================================================

# Disable Sentry entirely (recommended for local dev).
NEXT_PUBLIC_SENTRY_DISABLED=true

# Only needed when Sentry is enabled:
# NEXT_PUBLIC_SENTRY_DSN=       # https://****@****.ingest.sentry.io/****
# NEXT_PUBLIC_SENTRY_ORG=       # your-org
# NEXT_PUBLIC_SENTRY_PROJECT=   # your-project
# SENTRY_AUTH_TOKEN=            # sntrys_**** (source-map uploads at build time)

# =================================================================
# Build
# =================================================================
# Set to "true" for Docker/self-hosted deployments (standalone output).
# BUILD_STANDALONE=true
